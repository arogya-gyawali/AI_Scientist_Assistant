"""Stage 1 — Lit Review pipeline.

Self-contained module: everything stage-1-specific lives here, importing only
from `src/` (shared types, clients, lib). Designed so a teammate can
containerize this stage by copying `src/` (the shared backbone) plus this
folder.
"""
